# ===========================================
# 選情系統 - 後端環境變數設定
# ===========================================

# 應用程式設定
NODE_ENV=development
PORT=3001
API_PREFIX=api/v1

# ===========================================
# 資料庫設定
# ===========================================
# PostgreSQL 連線字串（需啟用 PostGIS 擴展）
# 格式: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://postgres:password@localhost:5432/election_system?schema=public"

# ===========================================
# Redis 設定
# ===========================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ===========================================
# JWT 設定
# ===========================================
# 請使用強密碼，建議使用 openssl rand -base64 32 產生
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# ===========================================
# LINE Login 設定
# ===========================================
# 
# 設定步驟：
# 1. 前往 LINE Developers Console: https://developers.line.biz/console/
# 2. 建立或選擇一個 Provider
# 3. 建立一個「LINE Login」類型的 Channel
# 4. 在 Channel 基本設定中取得：
#    - Channel ID
#    - Channel secret
# 5. 在「LINE Login」設定中，將 Callback URL 設為：
#    http://localhost:3000/login （開發環境）
#
# 注意：開發環境可使用 /auth/dev-login 端點測試，無需真實 LINE 帳號

LINE_CHANNEL_ID=your-line-channel-id
LINE_CHANNEL_SECRET=your-line-channel-secret

# ===========================================
# LINE Messaging API 設定（可選）
# ===========================================
# 用於發送推播通知
# 需要建立 Messaging API Channel 並取得 Access Token
LINE_MESSAGING_ACCESS_TOKEN=your-line-messaging-access-token

# ===========================================
# Google Maps API 設定（可選）
# ===========================================
# 用於地址轉座標（Geocoding）
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# ===========================================
# Google Calendar API 設定
# ===========================================
# 
# 設定步驟：
# 1. 前往 Google Cloud Console: https://console.cloud.google.com/
# 2. 建立或選擇專案
# 3. 啟用 Google Calendar API:
#    - 前往 APIs & Services > Library
#    - 搜尋 "Google Calendar API" 並啟用
# 4. 建立 OAuth 2.0 憑證:
#    - 前往 APIs & Services > Credentials
#    - 點擊 "Create Credentials" > "OAuth client ID"
#    - 選擇 "Web application"
#    - 設定授權重新導向 URI:
#      開發環境: http://localhost:3001/api/v1/google/callback
#      正式環境: https://your-domain.com/api/v1/google/callback
# 5. 複製 Client ID 和 Client Secret 到下方
#
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3001/api/v1/google/callback

# ===========================================
# 加密設定
# ===========================================
# 用於敏感資料加密，必須為 32 字元
# 建議使用 openssl rand -base64 32 產生
ENCRYPTION_KEY=your-32-character-encryption-key

# ===========================================
# CORS 設定
# ===========================================
CORS_ORIGIN=http://localhost:3000

# ===========================================
# 檔案上傳設定
# ===========================================
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# ===========================================
# 社群分享設定（可選）
# ===========================================
# 用於自動發佈相簿到社群平台

# Facebook（Graph API）
# 需要建立 Facebook App 並取得粉絲專頁的 Page Access Token
FACEBOOK_APP_ID=
FACEBOOK_APP_SECRET=
FACEBOOK_PAGE_ID=
FACEBOOK_PAGE_ACCESS_TOKEN=

# X (Twitter) API v2
# 需要建立 Twitter Developer App 並取得 OAuth 1.0a 憑證
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=

# Instagram Business Account
# 透過 Facebook Graph API 操作，需要與 Facebook App 配合
INSTAGRAM_BUSINESS_ACCOUNT_ID=

# 前端網站 URL（用於社群分享連結）
NEXT_PUBLIC_SITE_URL=https://election-demo-rouge.vercel.app

# ===========================================
# 日誌設定
# ===========================================
LOG_LEVEL=debug
