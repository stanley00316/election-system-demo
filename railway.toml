# =============================================
# Railway 部署設定 - 選情系統 API
# =============================================

[build]
dockerfilePath = "docker/Dockerfile.api"

[deploy]
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
